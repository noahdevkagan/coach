# Anarlog fork (added as submodule in Phase 2, not vendored)
/anarlog/

# Transcripts / meeting data — never commit (local-first, zero-telemetry)
*.transcript
*.wav
*.m4a
data/
transcripts/
**/private/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Node
node_modules/
dist/
build/

# Rust
target/

# Ollama binaries (too large for GitHub)
MeetingCoach/MeetingCoach/Resources/ollama/

# Xcode user state
*.xcuserstate
xcuserdata/

# macOS / editors
.DS_Store
.idea/
.vscode/
*.swp
tests/asr/audio/
tests/asr/.build/
tests/asr/.out/
tests/nudges/.build/
